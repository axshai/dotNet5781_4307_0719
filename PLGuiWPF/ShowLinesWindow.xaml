<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PLGuiWPF"
        xmlns:BO="clr-namespace:BO;assembly=BL" x:Class="PLGuiWPF.ShowLinesWindow"
        mc:Ignorable="d"
        Title="ShowLinesWindow" Height="450" Width="800" Loaded="Window_Loaded">

    <Window.Resources>
        <CollectionViewSource x:Key="busLineBOViewSource" d:DesignSource="{d:DesignInstance {x:Type BO:BusLineBO}, CreateList=True}"/>
    </Window.Resources>
    <DataGrid x:Name="busLineBODataGrid" AutoGenerateColumns="False" EnableRowVirtualization="True" ItemsSource="{Binding Source={StaticResource busLineBOViewSource}}" Margin="85,58,66.6,39" RowDetailsVisibilityMode="VisibleWhenSelected">
        <DataGrid.Columns>
            <DataGridTextColumn x:Name="idColumn" Binding="{Binding Id}" Header="Id" Width="SizeToHeader"/>
            <DataGridTextColumn x:Name="lineNumberColumn" Binding="{Binding LineNumber}" Header="Line Number" Width="SizeToHeader"/>
        </DataGrid.Columns>
    </DataGrid>

</Window>
